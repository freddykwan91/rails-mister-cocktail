<%= simple_form_for ([@cocktail, @dose]) do |f| %>
  <%= f.input :ingredient_id, collection: @ingredients %> <%= f.input :description %>
  <%= f.button :submit, 'Submit', class:'btn btn-primary' %>
<% end %>
